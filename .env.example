# Server port (most PaaS providers inject this automatically)
PORT=3000

# --- Optional LLM fallback ---------------------------------------------
# The service works fully without these (pure rules-based classification).
# If you want low-confidence "other" tickets to get a second opinion from
# Claude, set both of the following:
LLM_FALLBACK_ENABLED=false
ANTHROPIC_API_KEY=
